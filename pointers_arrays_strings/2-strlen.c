#include "main.h"
#include <string.h>

/**
 * _strlen - entry point
 *
 * @s: char variable
 * Description: function that returns the length of a string.
 * Return: a if success
*/

int _strlen(char *s)
{
int a;

for (a = 0; (s[a] != '\0'); a++)
{
}
return (a);
}
