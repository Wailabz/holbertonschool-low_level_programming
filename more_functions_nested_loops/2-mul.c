#include <stdlib.h>
#include <stdio.h>

/**
  * mul - Entry point
  * @a: firt int to return
  * @b: second int to return
  * Return: Always 0 (Success)
*/
int mul(int a, int b)
{
int produit = a * b;
return (produit);
}
